// Đây là file mẫu, bạn cần thay thế bằng logic gọi Gemini API thực tế
module.exports = {
  translateText: async (text, from, to) => {
    // TODO: Gọi Gemini API thực tế
    return `[${to}] ${text}`; // Trả về giả lập
  },
}; 